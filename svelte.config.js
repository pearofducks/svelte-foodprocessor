// Minimal Svelte config. No preprocessors are used (global CSS, Svelte 5).
// Its presence silences vite-plugin-svelte's "no Svelte config found" notice and
// gives editor tooling a config to read.
export default {}
